package org.aston.exercise.elements.title;

import org.aston.exercise.elements.base.BaseElement;
import org.openqa.selenium.By;

public class TitleElement extends BaseElement {
    public TitleElement(By locator, String name) {
        super(locator, name);
    }
}
